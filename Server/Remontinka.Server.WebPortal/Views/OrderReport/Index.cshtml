@model Remontinka.Server.WebPortal.Models.CustomReport.OrderReportModel

@{
    Layout = "~/Views/Shared/_PrintLayout.cshtml";
}

@if (!string.IsNullOrWhiteSpace(Model.Error))
{
    @Html.Label(string.Empty, Model.Error)
}
else
{
    @Html.Raw(Model.Report)
}