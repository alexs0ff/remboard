<xctk:ChildWindow x:Class="Remontinka.Client.Wpf.View.CustomReportPreview"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                 xmlns:xctk="http://schemas.xceed.com/wpf/xaml/toolkit" mc:Ignorable="d" 
             Height="500" Width="800"
                  
                  >
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition Height="80"/>
        </Grid.RowDefinitions>
        <WebBrowser x:Name="webBrowser"/>
        <StackPanel Orientation="Horizontal" Grid.Row="1" Grid.Column="0" HorizontalAlignment="Right" Margin="0,20,10,20">
            <Button x:Name="printButton" Width="100" Height="30" >
                <TextBlock Text="Распечатать"/>
            </Button>
            <Button x:Name="closeButton" Margin="10,0,0,0" Width="100" Height="30" >
                <TextBlock Text="Закрыть"/>
            </Button>
        </StackPanel>
    </Grid>
</xctk:ChildWindow>
